{
  "name": "next-azure-sandbox-api",
  "version": "1.0.0",
  "description": "Api for next-azure-sandbox",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "eslint": "npx eslint **/*.js",
    "typecheck": "npx tsc --noEmit",
    "lint": "npm run eslint && npm run typecheck"
  },
  "devDependencies": {
    "@azure/functions": "^1.2.3",
    "typescript": "^4.0.0"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "uuid": "^8.3.2"
  }
}
